# 本地开发用密钥文件。复制为 .dev.vars 并填入真实值。
# .dev.vars 已在 .gitignore 中，绝不会进仓库。
# `npx wrangler dev` 会自动加载 .dev.vars，无需 wrangler login / 无需 Cloudflare 账号即可本地联调。
#
# 重要：本文件（.dev.vars.example）会被 git 追踪并入库，只能放占位符。
#       真实密钥只写进 .dev.vars（已 gitignore），绝不写回本文件。

# GitHub OAuth App 的 Client ID / Secret（在 GitHub 创建 OAuth App 后得到，见下方步骤）
GH_CLIENT_ID=iv1_xxxxxxxxxxxxxxxx
GH_CLIENT_SECRET=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 会话 JWT 签名密钥：至少 32 字节随机串。可用 PowerShell 生成：
#   -join ((1..48) | ForEach-Object { (("{0:x}" -f (Get-Random -Max 16)) ) })
# 或随便敲一串长随机字符
SESSION_SECRET=请改成至少32字节的随机字符串

# 作者白名单：你的 GitHub 数字 user id（注意是数字，不是用户名）
# 获取方法（任选其一）：
#   a) 浏览器打开 https://api.github.com/users/你的用户名 ，看 "id" 字段
#   b) 有 PAT 的话：curl -H "Authorization: Bearer <你的PAT>" https://api.github.com/user ，看 "id"
#   c) 第一次登录时若不在白名单会报错；可先留空跳过白名单？不行，留空=谁都不能发，必须填
ALLOWED_GH_IDS=12345678

# 目标仓库
GH_REPO=zglstudylinux/personal-blog

# 用于提交内容/创建 PR 的 Personal Access Token
# 经典 PAT：需要 repo 权限（勾选 repo 即可，含 contents + pull_requests）
# 或细粒度 PAT：只授权本仓库，权限 Contents=Read and write + Pull requests=Write
GH_API_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
